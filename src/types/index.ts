export type NavItem = {
  label: string;
  href: string;
};

export type BudgetStep = {
  title: string;
  description: string;
};

export type GalleryItem = {
  title: string;
  alt: string;
  image: string;
  orientation: "portrait" | "landscape";
};
